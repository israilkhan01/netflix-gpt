/** @type {import('next').NextConfig} */
const nextConfig = {
    // output:'export',
    images:{
        domains:["cdn.cookielaw.org","https://assets.nflxext.com"]
    },
};

export default nextConfig;
